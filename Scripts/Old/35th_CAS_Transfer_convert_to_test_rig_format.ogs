// This script is used in CAS data. It intends to create an extra "Energy column" as column B in all the workbooks in the folder. It loops over all the workbooks, copies the transmission data from column B to column C,
// and then creates a column B equal to energy, which is 1240/col(A).
[Main]


doc -ef LB { // loop over all workbooks in folder
	page.title = 1;  // Let the workbook title shows Long Name only (1 = Long Name, 2 = short name, 3 = both)
	wks.colwidth = 9;// set all col width to 9	
	if (wks.ncols != 3) 
		{
			wks.ncols=3; // set total column number to 3
		}
// Write long name, units, comments, and parameter1 of column B
col(C) = col(B);
col(C)[L]$ = col(A)[L]$; // Copy long name of col(A) to column C.
col(B)[L]$ = "Energy";
col(B)[U]$ = "eV";
col(B)[O]$ = "1240/col(A)";
col(A)[L]$ = "Wavelength";
col(A)[U]$ = "nm";
			
}